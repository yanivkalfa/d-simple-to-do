import PropTypes from 'prop-types';

/*
export const NationalResourceType = PropTypes.shape({
  _id: PropTypes.string.isRequired,
  profilePicture: PropTypes.string.isRequired,
  nationalResource: PropTypes.number.isRequired
});

export const UserType = PropTypes.shape({
  name: PropTypes.string,
  profilePicture: PropTypes.string,
  profileType: PropTypes.string,
  _id: PropTypes.string
});

export const AuthorType = PropTypes.shape({
  _id: PropTypes.string,
  firstName: PropTypes.string,
  lastName: PropTypes.string,
  profilePicture: PropTypes.string
});

export const CommentType = PropTypes.shape({
  _id: PropTypes.string,
  comment: PropTypes.string,
  timestamp: PropTypes.string,
  author: AuthorType
});

export const MediaItemType = PropTypes.shape({
  _id: PropTypes.string,
  title: PropTypes.string,
  referenceURL: PropTypes.string,
  mediaURL: PropTypes.string,
  mediaType: PropTypes.string,
  isPublic: PropTypes.bool,
  description: PropTypes.string,
  comments: PropTypes.arrayOf(CommentType)
});

export const BoardType = PropTypes.shape({
  boardCover: PropTypes.string,
  boardType: PropTypes.string,
  category: PropTypes.string,
  comments: PropTypes.arrayOf(CommentType),
  created: PropTypes.string,
  isPublic: PropTypes.bool,
  media: PropTypes.arrayOf(MediaItemType),
  sharedWith: PropTypes.arrayOf(PropTypes.string),
  title: PropTypes.string,
  user: UserType
});

export const UserBoardsType = PropTypes.shape({
  personal: PropTypes.arrayOf(BoardType),
  business: PropTypes.arrayOf(BoardType),
  sharedWithMe: PropTypes.arrayOf(BoardType)
});
*/
